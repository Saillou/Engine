#pragma once

// To avoid glfw include
enum Key {
	Space      = 32,

	ArrowRight = 262,
	ArrowLeft  = 263,
    ArrowDown  = 264,
    ArrowUp    = 265,
};

enum Button {
	Left   = 0,
	Right  = 1,
	Middle = 2,
};